spring:
  application:
    name: proxy-service
  cloud:
    config:
      uri: http://localhost:8088

  output:
    ansi:
      enabled: detect

hystrix:
  command:
    myusers-service:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 10000