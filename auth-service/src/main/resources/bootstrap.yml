spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: http://localhost:8088
      fail-fast: true
      password: 1234
      username: user
    consul:
      host: localhost
      port: 8500
      discovery:
        instance-id: ${spring.application.name}:${server.port}

server:
  port: 8090